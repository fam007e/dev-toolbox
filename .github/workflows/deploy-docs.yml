name: Deploy Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'book.toml'
      - 'README.md'
      - 'CONTRIBUTING.md'
      - 'SECURITY.md'
      - 'CHANGELOG.md'
      - '.github/workflows/deploy-docs.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Install mdBook
        run: |
          mkdir bin
          curl -sSL https://github.com/rust-lang/mdBook/releases/download/v0.4.40/mdbook-v0.4.40-x86_64-unknown-linux-gnu.tar.gz | tar -xzC bin
          echo "$(pwd)/bin" >> $GITHUB_PATH

      - name: Prepare docs
        run: |
          # Copy files and fix links, and remove the redundant "View Online Documentation" link
          sed 's|docs/||g' README.md | grep -v "View Online Documentation" > docs/introduction.md
          cp CONTRIBUTING.md docs/CONTRIBUTING.md
          cp SECURITY.md docs/SECURITY.md
          cp CHANGELOG.md docs/CHANGELOG.md
          cp CODE_OF_CONDUCT.md docs/CODE_OF_CONDUCT.md

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Build with mdBook
        run: mdbook build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./book

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
